// Adds two integers.
fn add(a: i32, b: i32) -> i32 {
    // Perform addition
    a + b
}

fn main() {
    let x = 5;
    let y = 3;
    let result = add(x, y);
    println!("The sum of {} and {} is: {}", x, y, result);
}
